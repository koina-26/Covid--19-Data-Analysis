# -*- coding: utf-8 -*-
"""Covid -19 dataset analysis.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1ll2UH1CNRWziTvDBqp3dCtwp_zhIOqVj
"""

import pandas as pd

from google.colab import files
uploaded = files.upload()
file_name = list(uploaded.keys())[0]
data = pd.read_csv(file_name)

data.head()

data

data.count()

data.isnull().sum()

import seaborn as sns

import matplotlib.pyplot as plt

sns.heatmap(data.isnull())

# Show the number of confirmed deaths and recovered cases in each region

data.groupby('Region')[['Confirmed','Deaths','Recovered']].sum().sort_values(by='Confirmed', ascending = False)

# Remove all the records where the confirmed cases is less than 10

data.head(2)

data[data.Confirmed < 10]

data[~ (data.Confirmed < 10)]

data.head(20)

# In which region was the maximum number of confirmed cases recorded\
data.head(2)

data.groupby("Region").Confirmed.sum().sort_values(ascending = False).head(10)

# In which region was the minimum number of death cases recorded
data.head(2)

data.groupby("Region").Deaths.sum().sort_values(ascending = True).head(20)

# How many confirmed deaths and recovered cases were reported from India till April 2020
data.head(2)

data.head(2)

data[data.Region == ' India']

# Sort the entire data with respect to number of confirmed cases in ascending order
data.head(2)

data.sort_values( by = ['Confirmed'] , ascending = True).head(50)

# Sort the entire data with respect to number of recovered cases in descending order
data.head(2)

data.sort_values( by = ['Recovered'] , ascending = False).head(50)